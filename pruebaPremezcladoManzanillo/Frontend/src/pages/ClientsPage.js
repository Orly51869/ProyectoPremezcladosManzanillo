// Archivo: src/pages/ClientsPage.js
// Propósito: Página que muestra la lista de clientes. Contenedor mínimo que renderiza el componente `ClientList`.

import React from "react";
import ClientList from "../sections/dashboard/ClientList";

const ClientsPage = () => {
  return <ClientList />;
};

export default ClientsPage;
